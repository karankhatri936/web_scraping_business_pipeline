[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
markers =
    browser: tests that start a real Chromium instance (still offline)
filterwarnings =
    ignore::DeprecationWarning
