# --- Runtime dependencies ---
# Browser automation (Chromium). After install run: playwright install chromium
playwright>=1.49
# Data cleaning / transformation / analytics
pandas>=2.2
# Excel report generation
openpyxl>=3.1.2
# Scheduled execution
APScheduler>=3.10,<4
# .env configuration loading
python-dotenv>=1.0

# --- Testing ---
pytest>=8.0
pytest-cov>=5.0
